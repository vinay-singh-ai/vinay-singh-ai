# snake.yml — Generates contribution snake animation
# CRITICAL FIX: requires `permissions: contents: write` to push to output branch
# Trigger manually: Actions tab → "Generate Snake Animation" → "Run workflow"
# After first run, uncomment the snake <img> block in README.md

name: Generate Snake Animation

on:
  schedule:
    - cron: "0 0 * * 0"   # Every Sunday at midnight UTC
  workflow_dispatch:        # Manual trigger from Actions tab

permissions:
  contents: write           # Required to push SVG output to `output` branch

jobs:
  generate:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Generate snake animation (light + dark theme)
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: vinay-singh-ai
          outputs: |
            dist/github-snake.svg
            dist/github-snake-dark.svg?palette=github-dark

      - name: Push animation SVGs to output branch
        uses: crazy-max/ghaction-github-pages@v3
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
